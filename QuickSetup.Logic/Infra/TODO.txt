Done:
	* add field - FileHash (to check if existing file is exactly the one we are looking for)
	* Log4net output will be availble.
	* fix grid row height - buttons are trimmed
	* add two buttons to the top - "Add" and "Remove"
	* add SingleApp window 
	* when Edit or Add button is clicked show SingleApp window 
	* each app will have Checkbox, name, install button, edit button, Status field
	* force this to run as admin (app manifest?)


TODO:
* statusItem is white in design mode
* status field: 
	> V - installed
	> Blank - if not installed
	> ! - if setup file is missing	
	> ? - unknown issue

* to display fake progress - check if setup file is in use

* link to open "install new lang" - run "Lpksetup"
* ability to set lang for non-unicode program or open this screen ("region")
https://gallery.technet.microsoft.com/scriptcenter/Powershell-Set-language-981df2b0
control.exe "intl.cpl,,/f:`"c:\myregionalcfg.xml`""

-- myregionalcfg.xml content start --
<gs:GlobalizationServices xmlns:gs="urn:longhornGlobalizationUnattend">  
	<!--User List--> 
	<gs:UserList> 
		<gs:User UserID="Current" CopySettingsToDefaultUserAcct="true" CopySettingsToSystemAcct="true"/>  
	</gs:UserList> 
 
	<!-- This sets system locale to Hebrew --> 
	<gs:SystemLocale Name="he-IL" /> 
</gs:GlobalizationServices>
-- myregionalcfg.xml content end --


* file association window
* default browser
* windows activation status
* auto refresh every X seconds
* windows version at bottom + 86/64
* find installed toolbars and display warning
* chrome adblock?
* ff adblock?
* uac status + disable silent UAC with warning that Metro apps won't work

    Browse to registry key HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System
    Change EnableLUA to 0
    Install Calculator that works on Non-Metro mode from oldcalcwin10_winaero.com_1995.zip (or http://winaero.com/download.php?view.1795)
    Restart

* get public IP display at bottom
* Take ownership - install/uninstall
* classic shell setup






--- Wont be done ----
X add field - Last modified date,Pc\User
X Idea - generic RegKey to detect installed version. impossible in most cases 
