<controls:MetroWindow x:Class="QuickSetup.UI.Views.MainView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:converters="clr-namespace:QuickSetup.UI.Resources.Converters"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:viewModel="clr-namespace:QuickSetup.UI.ViewModel"
                      xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
                      Title="Quick Setup"
                      Width="900"
                      Height="350"
                      d:DataContext="{d:DesignInstance Type=viewModel:MainViewModel,
                                                       IsDesignTimeCreatable=True}"
                      BorderThickness="5"
                      DataContext="{Binding Path=Main,
                                            Source={StaticResource Locator}}"
                      ResizeMode="CanResizeWithGrip"
                      mc:Ignorable="d">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="CommandBinding_OnExecuted" />
    </Window.CommandBindings>
    <Window.Resources>
        <ResourceDictionary>
            <!--
                list of MahApps icons
                http://www.barth-dev.de/misc/stack/mahapps-icons-list/
            -->

            <!--<VisualBrush x:Key="SaveIcon" Visual="{StaticResource appbar_save}" />-->
            <converters:SoftwareStatusToImageConverter x:Key="MySoftwareStatusToImageConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel>
        <!--  Menu at the top  -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Command="{Binding Path=LoadAllApps, Mode=OneWay}" Header="Load">
                    <MenuItem.Icon>
                        <Rectangle Width="20" Height="20">
                            <Rectangle.Fill>
                                <VisualBrush Visual="{StaticResource appbar_book_open}" />
                            </Rectangle.Fill>
                        </Rectangle>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Command="{Binding Path=SaveAllApps, Mode=OneWay}" Header="Save">
                    <MenuItem.Icon>
                        <Rectangle Width="20" Height="20">
                            <Rectangle.Fill>
                                <VisualBrush Visual="{StaticResource appbar_save}" />
                            </Rectangle.Fill>
                        </Rectangle>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Command="Close"
                          Header="E_xit"
                          InputGestureText="Alt+F4">
                    <MenuItem.Icon>
                        <Rectangle Width="20" Height="20">
                            <Rectangle.Fill>
                                <VisualBrush Visual="{StaticResource appbar_close}" />
                            </Rectangle.Fill>
                        </Rectangle>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem HorizontalContentAlignment="Right">
                <TextBlock HorizontalAlignment="Right" FontSize="8">
                    <Run Text="This Project is using the following products:" />
                    <LineBreak />
                    <Run Text="MS .Net, Log4Net, MahApps.Metro, Xceed WPF, GalaSoft.MvvmLight, FlatIcon.com" />
                </TextBlock>
            </StatusBarItem>
        </StatusBar>

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Grid of apps  -->
            <DockPanel Grid.Column="0">
                <ToolBar DockPanel.Dock="Top">
                    <Button Command="{Binding Path=AddNewAppCommand}">
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="20" Height="20">
                                <Rectangle.Fill>
                                    <VisualBrush Visual="{StaticResource appbar_add}" />
                                </Rectangle.Fill>
                            </Rectangle>
                            <TextBlock VerticalAlignment="Center" Text="Add New" />
                        </StackPanel>
                    </Button>

                    <Separator />
                    <Button Command="{Binding Path=LoadAllApps, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="20" Height="20">
                                <Rectangle.Fill>
                                    <VisualBrush Visual="{StaticResource appbar_book_open}" />
                                </Rectangle.Fill>
                            </Rectangle>
                            <TextBlock VerticalAlignment="Center" Text="Load" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding Path=SaveAllApps, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="20" Height="20">
                                <Rectangle.Fill>
                                    <VisualBrush Visual="{StaticResource appbar_save}" />
                                </Rectangle.Fill>
                            </Rectangle>
                            <TextBlock VerticalAlignment="Center" Text="Save" />
                        </StackPanel>
                    </Button>
                </ToolBar>

                <xcdg:DataGridControl Margin="5"
                                      AutoCreateColumns="False"
                                      AutoScrollCurrentItem="All"
                                      ItemsSource="{Binding Path=SoftwareList,
                                                            Mode=OneWay}"
                                      SelectedItem="{Binding Path=SelectedSoftware,
                                                             Mode=TwoWay,
                                                             UpdateSourceTrigger=PropertyChanged}">

                    <xcdg:DataGridControl.View>
                        <xcdg:TableflowView ContainerHeight="35" ShowRowSelectorPane="False" />
                    </xcdg:DataGridControl.View>

                    <xcdg:DataGridControl.Columns>

                        <xcdg:Column Title="Install?"
                                     Width="60"
                                     MinWidth="50"
                                     MaxWidth="70"
                                     FieldName="IsChecked">
                            <xcdg:Column.CellContentTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{xcdg:CellEditorBinding}" />
                                </DataTemplate>
                            </xcdg:Column.CellContentTemplate>
                        </xcdg:Column>

                        <xcdg:Column Title="Software Name"
                                     Width="180"
                                     FieldName="OriginalModel.SoftwareName"
                                     IsMainColumn="True"
                                     ReadOnly="True">
                            <xcdg:Column.CellContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding}" ToolTip="{Binding Path=OriginalModel.NotesToolTip, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <Image Source="{Binding}" />
                                    </StackPanel>
                                </DataTemplate>
                            </xcdg:Column.CellContentTemplate>
                        </xcdg:Column>

                        <xcdg:Column Title="Language"
                                     Width="70"
                                     FieldName="OriginalModel.LangCodeIso6392"
                                     IsMainColumn="False"
                                     ReadOnly="True" />

                        <xcdg:Column Title="Setup Folder"
                                     Width="180"
                                     FieldName="OriginalModel.SetupFolder"
                                     IsMainColumn="True"
                                     ReadOnly="True"
                                     Visible="{Binding Path=IsDev,
                                                       Mode=OneWay}" />

                        <xcdg:Column Title="Status"
                                     FieldName="Status"
                                     IsMainColumn="False"
                                     ReadOnly="True">
                            <xcdg:Column.CellContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Converter={StaticResource ResourceKey=MySoftwareStatusToImageConverter}, Mode=OneWay}" />
                                    </StackPanel>
                                </DataTemplate>
                            </xcdg:Column.CellContentTemplate>
                        </xcdg:Column>

                        <xcdg:UnboundColumn Title="Operations"
                                            MinWidth="180"
                                            FieldName="FakeColumnName">
                            <xcdg:UnboundColumn.CellContentTemplate>

                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <!--
                                            Commands are not working with Xceed UnboundColumn. no idea why
                                            Command="{Binding Path=ParentRow.Row.InstallCommand}"
                                        -->
                                        <Button MinWidth="50"
                                                Margin="3,0,3,0"
                                                Background="Yellow"
                                                Click="ButtonInstall_OnClick"
                                                Padding="0">
                                            <Rectangle Width="20" Height="20">
                                                <Rectangle.Fill>
                                                    <VisualBrush Visual="{StaticResource appbar_cabinet_in}" />
                                                </Rectangle.Fill>
                                            </Rectangle>
                                        </Button>

                                        <!--
                                            Commands are not working with Xceed UnboundColumn. no idea why
                                            Command="{Binding Path=DataGridContext.EditAppCommand}"
                                        -->
                                        <Button MinWidth="50"
                                                Margin="3,0,3,0"
                                                Click="ButtonEdit_OnClick"
                                                Padding="0">
                                            <Rectangle Width="20" Height="20">
                                                <Rectangle.Fill>
                                                    <VisualBrush Visual="{StaticResource appbar_edit}" />
                                                </Rectangle.Fill>
                                            </Rectangle>
                                        </Button>

                                        <Button MinWidth="50"
                                                Margin="3,0,3,0"
                                                Click="ButtonRemove_OnClick"
                                                Padding="0">
                                            <Rectangle Width="20" Height="20">
                                                <Rectangle.Fill>
                                                    <VisualBrush Visual="{StaticResource appbar_app_remove}" />
                                                </Rectangle.Fill>
                                            </Rectangle>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </xcdg:UnboundColumn.CellContentTemplate>
                        </xcdg:UnboundColumn>
                    </xcdg:DataGridControl.Columns>
                </xcdg:DataGridControl>
            </DockPanel>

            <GridSplitter Grid.Row="0"
                          Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext"
                          ResizeDirection="Columns" />

            <Expander Grid.Row="0"
                      Grid.Column="2"
                      Margin="5"
                      ExpandDirection="Right">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Log:" />
                    <TextBox AcceptsReturn="True"
                             IsReadOnly="True"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             Text="{Binding Path=LogOutputToWindow,
                                            Mode=OneWay,
                                            UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
            </Expander>
        </Grid>
    </DockPanel>
</controls:MetroWindow>